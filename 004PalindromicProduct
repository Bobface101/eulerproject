for num in range(100,1000):
    string = str(num**2)
    if len(string) % 2 == 0:
        firstHalf = string[0:len(string)/2 + 1]
        secondHalf = string[len(num)/2:]
        print(string, firstHalf, secondHalf)

print("hi")
